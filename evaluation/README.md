# Evaluation

This directory will contain:

- retrieval ground truth and evaluation scripts;
- itinerary evaluation samples and results;
- comparisons between retrieval and prompting approaches.

Raw experiments belong in `notebooks/`; reusable evaluation logic should later be
moved into Python modules.
